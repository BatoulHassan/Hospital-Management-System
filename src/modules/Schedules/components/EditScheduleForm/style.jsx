import { Box } from '@mui/material'
import { styled } from '@mui/material/styles';

export const InputTimeBox = styled(Box)(() => ({
  marginBottom: '1rem', 
  display: 'flex',
  alignItems: 'center', 
  flexDirection: 'column'
}))